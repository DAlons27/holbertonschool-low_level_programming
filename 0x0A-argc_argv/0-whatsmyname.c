#include <stdio.h>
#include "holberton.h"

/**
 * main - imprimir el nombre del programa
 * @argc: numero de arguementos
 * @argv: array de argumentos
 *
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);

return (0);
}
