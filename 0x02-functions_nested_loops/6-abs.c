#include "holberton.hs"

/**
 * _abs - calcula el valor absoluto
 * @n: el numero el cual servira para calcular su valor absoluto
 * Return: devolucion del valor absoluto
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
